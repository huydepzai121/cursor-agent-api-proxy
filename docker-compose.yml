services:
  cursor-agent-api:
    build: .
    ports:
      - "${PORT:-4646}:4646"
    environment:
      - PORT=4646
      - CURSOR_API_KEY=${CURSOR_API_KEY}
      - PROXY_API_KEY=${PROXY_API_KEY:-}
    restart: unless-stopped
